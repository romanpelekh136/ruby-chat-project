<%= form_with model: [room, message], id: "new_message_form" do |f| %>
  <%= f.text_field :body, autofocus: true, placeholder: "Write new message...", required: true %>
  <%= f.submit "Send" %>
<% end %>
