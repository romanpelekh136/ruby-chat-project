<h1><%= @room.name %></h1>

<%= turbo_stream_from @room %>

<div id="messages">
  <% @messages.each do |message| %>
    <%= render message %>
  <% end %>
</div>

<%= render "messages/form", room: @room, message: @message %>
  
