<h1>Chat Rooms</h1>

<%= turbo_stream_from "rooms" %>

<%= form_with model: @room, id: "new_room_form" do |f| %>
  <%= f.text_field :name, placeholder:"Room name" %>
  <%= f.submit "Create" %>
<% end %>

<div id="rooms">
  <%@rooms.each do |room|%>
    <%= render room %>
  <%end%>
</div>
